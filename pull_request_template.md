Open an issue to add a new link.

All the links are built via a script I built off the CSV dump from an Airtable, so you can't edit those.

You can edit & submit a PR for other files (src/copy.js, etc) for improvements.
